package entities;

public class ModellingEngine {
    // on each step asks system if anything changed
    // runs the next step for every registered process
    // calculates processes run order
}
